# web-checkout

npm.

> **Synthetic fixture.** This repository exists to exercise the continuous
> vulnerability governance flow in `naoharu/cursor-governance-demo`. The
> dependency versions below are deliberately old. There is no application code.
